# Podman

Podman is a container engine to build, run and share containers. It supports Linux, Mac and Windows.

## Mac Setup

Containers are Linux they don't run on any other OS because the core functionality of containers are tied to Linux .
Hence, while initializing the machine on mac, it creates a VM with Linux.  
