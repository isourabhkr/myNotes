# Overview

The concept of concurrency requires an essential concept, pausing a long running execution and coming back to it once
the same has finished. Coroutines facilitates the same. 

## Async and Await
In Pythong `Async` is the way of defining a coroutine, and `Await` allows pausing a coroutine. 
